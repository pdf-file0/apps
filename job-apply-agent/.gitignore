node_modules/
dist/
coverage/
.env
*.log

# Run outputs are generated artifacts
runs/*
!runs/.gitkeep

# Local browser profile for live recon — may contain cookies/session data,
# must never be committed
.browser-profile/

# Never commit real candidate documents
documents/*
!documents/.gitkeep
